# security -- IMPLEMENTATION

> STATUS: EMPTY (Phase 0 scaffold). Fill from the IMPLEMENTATION template in `docs/56_contract_templates.md` at this subsystem's contract stage, BEFORE any code (contracts-before-code gate, `docs/70` s4). The consistency check (`56`) must pass before this subsystem is CLEARED.

