# BUILD TRACKER — Charter House

**Append-only build ledger** (docs/70 §3). Newest entries at the bottom. This file +
an always-green `main` are the complete resumable state of the build (docs/70 §9).
Each entry records: date, phase, workstream, action, and any interface-freeze unlocked.

Legend — actions: `START` · `CONTRACTS-CLEARED` · `MERGED` · `PHASE-EXIT` ·
`INTERFACE-FREEZE` · `BLOCKING-IMPOSSIBILITY`.

| Date | Phase | Workstream | Action | Notes / Unlocks |
|---|---|---|---|---|
| 2026-07-03 | 0 | A0 Scaffold | START | Repository foundation per docs/53 Phase 0. |
| 2026-07-03 | 0 | A0 Scaffold | MERGED | Tree per docs/31 (structure test green, 107 passed); 13 subsystem packages × 4 empty contract docs; .gitignore/.env.example/pyproject; uv 0.11.26 + pinned pytest 8.3.4; git init (main); merge gates wired (scripts/ci.ps1 + pre-commit hook). |
| 2026-07-03 | 0 | Phase 0 | PHASE-EXIT | Exit criteria met: disjoint subsystem ownership (any agent creates its files without touching another's); merge gates (docs/63) active. **HALT** — awaiting founder go-ahead before opening Wave 0. |
| 2026-07-04 | 1–2 | Wave 0 | START | Founder go-ahead. Opened contract stage for A1 (Environment/S2), A2 (Config/S3), A3 (Ledger+Registry/S4), A11 (Logging+Test/S14+S15). |
| 2026-07-04 | 1–2 | Wave 0 | CONTRACTS-CLEARED | 20 contract docs + docs/WAVE0_CLEARANCE.md 56-consistency trace reviewed and **CLEARED by founder**. A1, A2, A3-Ledger, A3-Registry, A11 cleared to implement (tests-first, docs/70 §5). |
| 2026-07-04 | 1–2 | A3 Ledger/Registry (S4) | INTERFACE-FREEZE | **IF-1 frozen** (docs/52 §12): common envelope (docs/41 §1, verbatim in ledger/API.md) + event-type vocabulary (docs/41 §2) + `Ledger.append/read/replay/snapshot/restore` + `Registry.get/query`. **Unlocks A4 Lifecycle, A5 Governance/Security, A7 Memory.** Evolution: additive event types/fields = versioned no-bump; remove/rename/envelope-change = ICR (docs/43 §4). |
| 2026-07-04 | 1–2 | A2 Config (S3) | INTERFACE-FREEZE | **IF-2 (Config half) frozen**: `Config.get_route/get_model/get_provider/profile/budgets` + shared types Route/Model/Provider/Budgets (docs/40 §1). Router `LLMClient` half of IF-2 pending A6. **Unlocks A1 preflight, A6 Router (Config side).** |
| 2026-07-04 | 1–2 | A11 Logging/Test (S14+S15) | INTERFACE-FREEZE | Harness surface frozen: `Log.event`/`Telemetry.record` + fake signatures (InMemoryLedger == Ledger docs/40 §2) + invariant-manifest shape. Simulator shape-only (body deferred to S4/S5/S10/S12). |
| 2026-07-06 | 1–2 | A3 Ledger/Registry (S4) | START | `main` at Wave-0 contracts (0b72eb3). Branch `feat/a3-ledger`, tests-first per docs/70 §5. |
| 2026-07-06 | 1–2 | A3 Ledger/Registry (S4) | (impl, pre-gate) | Tests-first green: 122 S4 tests (60+40 property seeds) — INV-LEDGER replay==state, atomic+totally-ordered append, hash-chain tamper→fail-closed, snapshot→restore byte-identical, PII/token/type/cap/schema rejects. Built the frozen IF-1 shared types in `charterhouse/contracts/` (`events.py`, `state.py`) + `ledger/{store,ulid}.py` + `registry/facade.py`. Import-DAG + secret scan clean by hand (gates 5/10 still placeholders). Full CI green (229 tests). Docs synced (62). **HALT at merge gate — awaiting founder review before merge to `main`.** |
| 2026-07-07 | 1–2 | A3 Ledger/Registry (S4) | MERGED | Founder-reviewed at the gate. Review fix: replay now fails closed with `ProjectionError` (naming the venture) for a state-less venture instead of a bare `ValueError` (test-first). Findings 2 (lineage cap keyed on `venture_id`) + 3 (non-atomic `restore`) accepted as documented for the hardening pass (ledger/RISKS.md). Squash-merged `feat/a3-ledger`; **123 S4 tests green, main CI green**. **S4 is real** → A4 Lifecycle, A5 Governance/Security, A7 Memory unblocked against the live Ledger/Registry + IF-1. |
| 2026-07-07 | 1–2 | A5 Governance/Security (S6+S7) | START | Branch `feat/a5-governance` off `main` at A3 merge (408c37b). Opened contract stage for S6 Governance + S7 Security against live IF-1. Additive shared types first: contracts authz + IF-2 Config-half types (21983c1). |
| 2026-07-07 | 1–2 | A5 Governance/Security (S6+S7) | CONTRACTS-CLEARED | S6+S7 contract docs (API/TESTPLAN/IMPLEMENTATION/RISKS per subsystem, 94ed2cf) written and self-cleared in **auto mode via consistency-checks** — no separate founder-clearance gate ran (unlike Wave 0); founder review deferred to the merge gate (see MERGED row when recorded). Tests-first written same day — red for the right reason (b8aeafe, docs/70 §5). |
| 2026-07-07 | 1–2 | A5 Governance/Security (S6+S7) | INTERFACE-FREEZE | **IF-3 frozen** (docs/52 §12, docs/43 §3): Governance `classify/authorize/envelope_open/spend/send_budget_remaining` (governance/API.md) + Security `redact/scan/tag` signatures with the `Context.contains_pii` cloud-route guard (security/API.md, docs/40 §4). **Unlocks A6 Router pii-path (S8 enforcement of INV-PII-3), A7 Memory.** |
| 2026-07-09 | 1–2 | A5 Governance/Security (S6+S7) | (impl, pre-gate) | Implementation green (eb9c67c): S6 classify/envelope/tokens/send-budget + facade; S7 redact → independent scan → fail-closed CHECKPOINT (INV-PII-1/2), tag + `cloud_route_allowed` guard (INV-PII-3), `CheckpointError` names finding kinds only — never values. Full CI green (**324 tests**). **HALT at merge gate — awaiting founder review before merge to `main`.** |
| 2026-07-10 | 1–2 | A5 Governance/Security (S6+S7) | MERGED | Founder-reviewed at the gate (security/tag.py INV-PII-3 guard + security/checkpoint.py fail-closed pipeline verified in full; CONTRACTS-CLEARED row reworded honestly — auto-mode consistency-checks, no separate clearance gate; this MERGED review is the founder review). Squash-merged `feat/a5-governance` (2542277); **324 tests green, main CI green**. **S6+S7 are real, IF-3 backed by real code** → A6 Router pii-path and A7 Memory unblocked against live Governance/Security. |
| 2026-07-10 | 1–2 | A4 Lifecycle (S5) | START | Founder go-ahead (route: A4 after A5 merge). Branch `feat/a4-lifecycle` off `main` at A5 merge (7a25a8a). Builds docs/42 exactly, against live IF-1 (Ledger/Registry) + IF-3 (Gov). |
| 2026-07-10 | 1–2 | A4 Lifecycle (S5) | CONTRACTS-CLEARED | S5 contract docs (API/TESTPLAN/IMPLEMENTATION/RISKS, 4aad1f2) written and self-cleared in **auto mode via the docs/56 consistency check** — no separate founder-clearance gate (A5 precedent); founder review deferred to the merge gate. Open questions resolved in IMPLEMENTATION §6 (guards as ledger facts, judgment-guard envelope, active-days unit, additive pivot/OMW/pause seams). |
| 2026-07-10 | 1–2 | A4 Lifecycle (S5) | INTERFACE-FREEZE | **IF-4 frozen** (docs/43 §3: "Lifecycle transition API + `42` invariants"): `can_transition/transition/slots/clock` per docs/40 §3, `GuardResult/Result/SlotState/ActiveTime` shapes, the docs/42 §3 table + §2 WIP limits + §4 guard rules + §5 pivot semantics (INV-SM-1..6). Additive v1 seams: `pivot/grant_omw/pause/resume` (docs/43 §7). **Unlocks S10 Capability Framework, S12 Conductor.** |
| 2026-07-10 | 1–2 | A4 Lifecycle (S5) | (impl, pre-gate) | Tests-first red (e972bb9) → green (ada99e7): docs/42 §3 verbatim-as-data (26 rows, fidelity-tested), 15×15 legality matrix reject+log, WIP/slots, active-day clocks with pause, express restriction, pivot kill-and-fork + OMW lineage caps (all-or-nothing), TTL re-confirmation; 60 seeded property walks vs an independent oracle; Stress-Test A/B/C reproduced via the simulator over real S4+S6. Full CI green (**407 tests**). **HALT at merge gate — awaiting founder review before merge to `main`.** |
| 2026-07-10 | 1–2 | A4 Lifecycle (S5) | MERGED | Founder-reviewed at the gate: **transition guards + the INV-SM-5 lineage walk verified; invariants hold** (this MERGED review is the founder review, per the auto-mode contract stage). Housekeeping: `uv.lock` now tracked. Squash-merged `feat/a4-lifecycle` (8e5e12f); **407 tests green, main CI green**. **S5 is real, IF-4 backed by real code** → S10 Capability Framework and S12 Conductor unblocked against the live Lifecycle; the docs/63 gate-2 INV-SM harness can drop its placeholder. |
| 2026-07-11 | 1–2 | A2 Config (S3) | START | Founder go-ahead (Wave-0 trio: A1/A2/A11, each own branch, lighter review). Branch `feat/a2-config` off `main` at A4 merge (cf8a69d). Contract docs were **founder-CLEARED at Wave 0** (2026-07-04 CONTRACTS-CLEARED, docs/WAVE0_CLEARANCE.md) — re-ran the docs/56 consistency check in auto mode; refined config/IMPLEMENTATION §6 (routes.yaml carries default `budgets`+`routes`; embed-model-mismatch refusal lives in A1 preflight, which owns env+vector access — **consistency note for founder**). |
| 2026-07-11 | 1–2 | A2 Config (S3) | (impl, pre-gate) | Tests-first red → green: loader (schema strict-key validation, INV-CFG both clauses incl. fallback refs, profile/override precedence, located errors) + the immutable `Config` facade (docs/40 §1) + the real committed `config/*.yaml` (providers/models/routes + free/cheap-cloud/local-first profiles, zero-paid-on-`free`). Pins `pyyaml==6.0.2` (safe_load only). IF-2 Config half already frozen (contracts/config_types.py). Full CI green (**427 tests**). **HALT at merge gate.** |
| 2026-07-11 | 1–2 | A2 Config (S3) | MERGED | Founder-reviewed at the gate (INV-CFG confirmed; routes.yaml budgets+routes layout + embed-mismatch-in-A1 boundary flags accepted). Squash-merged `feat/a2-config` (65d99cc); **main CI green**. **S3 is real, IF-2 Config half backed by real code + committed `config/*.yaml`** → A1 preflight's real `Config.load` path and A6 Router role-resolution unblocked. |
| 2026-07-11 | 1–2 | A1 Environment (S2) | START | Founder go-ahead (Wave-0 trio: A1/A2/A11, each own branch, lighter review). Branch `feat/a1-environment` off `main` at A4 merge (cf8a69d). Contract docs **founder-CLEARED at Wave 0** (2026-07-04, docs/WAVE0_CLEARANCE.md); re-ran the docs/56 consistency check in auto mode. `Config` consumed via a `ConfigPort` stub (merged from A2 at merge time; docs/43 §2). |
| 2026-07-11 | 1–2 | A1 Environment (S2) | (impl, pre-gate) | Tests-first red → green: `preflight()` (the sole env reader) with the ordered docs/20 checks — one precise error each for missing env var / non-writable K: path / low C: headroom / Ollama unreachable / uninitialized vector store / **embed-model mismatch** (A1-owned; realizes docs/54 §S3's "embed-model mismatch refused" since Config reads no env/vector store — see config/IMPLEMENTATION §6) / unresolvable route (surfaces Config's error) — producing the frozen `EnvContext` (added to `contracts/env_context.py` with `PathKind`). K:-discipline guard refuses off-root growing writes; **static env-boundary scan** confirms `os.environ` lives only under `charterhouse/env/`; no-partial-boot fault matrix; zero-paid-on-`free`. Additive keyword seams keep `preflight()`'s frozen signature. Full CI green (**420 tests**). **HALT at merge gate.** |
| 2026-07-11 | 1–2 | A1 Environment (S2) | MERGED | Founder-reviewed at the gate (preflight one-precise-error checks + K:-discipline + env-boundary scan + embed-mismatch placement accepted). Squash-merged `feat/a1-environment` (e33158e); **main CI green**. **S2 is real** — `EnvContext` is the sole env reader → every subsystem takes paths/profile/endpoints from preflight, never env; A1's real `Config.load` path (A2 now merged) is live. |
| 2026-07-11 | 1–2 | A11 Logging/Test (S14+S15) | START | Founder go-ahead (Wave-0 trio). Branch `feat/a11-logging-test` off `main` at A4 merge (cf8a69d). Contract docs **founder-CLEARED at Wave 0** (2026-07-04, harness surface INTERFACE-FREEZE row); re-ran the docs/56 consistency check in auto mode. Now that S4/S5/S6/S7 are real, the Wave-0 "shape-only" harness gets real bodies. |
| 2026-07-11 | 1–2 | A11 Logging/Test (S14+S15) | (impl, pre-gate) | S14 Logging: `Log.event` (structured file sink) + `Telemetry.record` (`llm_call` ledger events) — distinct sinks; both redact secret/PII fields reusing the merged S7 `Scanner` (no drift). S15 harness: `FakeProvider/FakeEmbedder/Clock/InMemoryLedger/Simulator` (frozen signatures; **InMemoryLedger is a real-`Ledger` subclass over an ephemeral temp dir** — strongest anti-drift, founder consistency note) + `pii_corpus()`/`golden_set()` fixtures + the `invariant_manifest`. **Gate 2 goes LIVE**: `scripts/invariant_check.py` verifies every INV-SM-1..6 maps to a collectable test (docs/55 §4); ci.ps1 placeholder removed. Harness co-developed with its self-tests (docs/51 A11 — the fakes are the deliverable the self-tests exercise). Full CI green (**424 tests, gate 2 now active**). **HALT at merge gate.** |
| 2026-07-11 | 1–2 | A11 Logging/Test (S14+S15) | MERGED | Founder-reviewed at the gate (log redaction through the merged S7 scanner + InMemoryLedger-as-`Ledger`-subclass accepted). Squash-merged `feat/a11-logging-test`; **main CI green, gate 2 now LIVE** (INV-SM harness). **S14+S15 are real** → observability spine (`Log`/`Telemetry`) + shared fakes + invariant harness available to every subsystem; **Wave 0 complete** (A1/A2/A3/A11 + Wave-1 A4/A5 all merged). |
| 2026-07-11 | 3 | A6 Router (S8) | START | Founder go-ahead. Branch `feat/a6-router` off `main` at A11 merge (2c60e60). Builds docs/11 + docs/22 against real Config (IF-2 half), real S7 guard (IF-3), real Ledger/Telemetry (IF-1/S14). A11's `FakeProvider` is every transport — no network in A6 (live smoke non-gating). |
| 2026-07-11 | 3 | A6 Router (S8) | CONTRACTS-CLEARED | S8 contract docs (e9d8b9d) written and self-cleared in **auto mode via the docs/56 consistency check** — founder review deferred to the merge gate (A5 precedent). Key resolutions (IMPLEMENTATION §6): secrets via injected `key_lookup` at wiring time (env-boundary kept — docs/24 wording reconciled with docs/20); embeddings deferred to A7 (local-only); Grok = named OpenAI-compatible subclass; conservative budget fold (A11 cross-note: stamp `timestamp` in `Telemetry.record`). |
| 2026-07-11 | 3 | A6 Router (S8) | INTERFACE-FREEZE | **IF-2 COMPLETE** — the `LLMClient` half frozen (docs/52 §12): `LLMClient.call(role, messages, tools?, require?) -> LLMResponse` + `Require`/`LLMResponse` shapes + `Adapter.complete` signature (docs/40 §5); additive `critic_tier` + adapter `context` kwargs (docs/43 §7). **Unlocks A7 Memory and S10 Capability Framework against the full IF-2.** |
| 2026-07-11 | 3 | A6 Router (S8) | (impl, pre-gate) | Tests-first red (50635ba) → green: chain builder (config-order candidates, constraint merging, PII locality filter, budget tier guard, deterministic free/local degrade extension), failover loop (one attempt per candidate → `ProvidersExhausted` pause signal + `error` event), OpenAI-compat adapter + Anthropic/Gemini pure-translation shims + Grok subclass, **two-layer INV-PII-3 enforcement** (chain filter with `pii_block` audit + the merged S7 `require_cloud_allowed` guard inside every cloud adapter — a guard hit is a hard stop, never a failover hop), `llm_call` telemetry with catalog-priced cost (INV-ROUTE-4). Design note at the gate: under a PII tag the ROUTE-level `min_ctx` relaxes so the work runs locally (explicit `require.min_ctx` still enforced; IMPLEMENTATION §6.6). Interim `_catalog_ids` read of Config's table pending an additive `Config.models()` (RISKS R9, A2 cross-note). 20-seed failover property + committed-config integration (tag→local PII flow end-to-end). Full CI green (**495 tests**). **HALT at merge gate — PII-block + failover code presented for founder review.** |
| 2026-07-12 | 3 | A6 Router (S8) | MERGED | Founder-reviewed at the gate (2026-07-11): **PII cloud-block verified in both layers — chain-level locality filter with `pii_block` audit + the merged S7 `require_cloud_allowed` guard reused inside every cloud adapter, hard-stop semantics confirmed, zero cloud calls proven end-to-end; failover logic clean; INV-ROUTE-1..4 covered** (this MERGED review is the founder review, per the auto-mode contract stage). Squash-merged `feat/a6-router` (df5b28b); **495 tests green, main CI green** (gate 2 INV-SM harness OK). **S8 is real, IF-2 COMPLETE and backed by real code** → A7 Memory builds against the full IF-2 + IF-3; open follow-ups: additive `Config.models()` to retire the interim `_catalog_ids` read (router RISKS R9, A2 cross-note). |
| 2026-07-16 | 3 | A7 Memory (S9) | START | Founder go-ahead (re-run of the 2026-07-16 A7 prompt; the first session ended before work began). Branch `feat/a7-memory` off `main` at A6 merge (80720da). Builds docs/12 + docs/33 against real IF-1 (Ledger), IF-2 COMPLETE, IF-3 (S7 Scanner), A11 harness (`FakeEmbedder`). New pinned runtime dep `lancedb==0.34.0` (docs/33 frozen store; wheel verified on Python 3.14/win, cache-redirections-to-K: checked before install). |
| 2026-07-16 | 3 | A7 Memory (S9) | CONTRACTS-CLEARED | S9 contract docs (8a350e9) written and self-cleared in **auto mode via the docs/56 consistency check** — no separate founder-clearance gate ran; founder review deferred to the merge gate (A5/A4/A6 precedent). Key resolutions (IMPLEMENTATION §6): ranking weights as injected `RetrievalWeights` (additive `Config.memory` cross-note to A2, router-R9 pattern); doctrine from `vault/memory/DOCTRINE.md`, missing = legitimate pre-doctrine `""`; embed-model-mismatch = same reused S2 type + the SAME `EMBED_MODEL` marker A1 preflight reads (defense in depth, one artifact); INV-MEM-4 = **verification not redaction** (merged S7 `Scanner` gates every embed); promotion deterministic, doctrine proposals-only; `scope` kwarg as the additive S11 seam; embeddings Ollama-direct, NOT via `LLMClient`. |
| 2026-07-16 | 3 | A7 Memory (S9) | INTERFACE-FREEZE | **Memory surface frozen** (docs/12 "interface frozen early", docs/40 §6): `Memory.retrieve(task, k) -> WorkingSet` / `write_lesson(lesson) -> lesson_id` / `consolidate() -> ConsolidationReport` / `reindex(reason)` + `Embeddings.embed` + `TaskContext`/`Lesson`/`ScoredLesson(+components)`/`WorkingSet`/`ConsolidationReport` shapes (memory/API.md). Additive v1 seams (docs/43 §7): `write_lesson(..., scope=)`, `RetrievalWeights` injection, richer `components`. **Unlocks S10 Capability Framework retrieval stubs.** |
| 2026-07-16 | 3 | A7 Memory (S9) | (impl, pre-gate) | Tests-first red (84 S9 tests, NotImplementedError-for-the-right-reason) → green (e87e4fb): LanceDB store on tmp/K: with dual pin (marker + per-row), mismatch refused via reused S2 type, guarded deterministic reindex (temp-table swap; blank reason = `UnguardedReindex`); docs/33 weighted retrieval (Doctrine always; retired/superseded excluded pre-ranking; `max_k` clamp so the full store is never dumped; per-component score breakdown; 20-seed property vs an independent f32-faithful oracle); deterministic consolidation (dup-merge/retire/promote-to-playbook idempotent, doctrine proposals-only, ledger byte-identical + rebuild-from-`lesson_written` reversibility); **INV-MEM-4 enforced as a hard pre-embed gate** — corpus-parametrized zero-embed refusals (kinds-only errors) + real-S7-CHECKPOINT→memory integration flow; kill→salvage→lesson→retrievable-at-next-gate (docs/54 §S9). Letters-only generated lesson ids (S4 digit-run pre-check, RISKS R7). Full CI green (**579 tests**, gate 2 harness OK). **HALT at merge gate — retrieval ranking + no-PII-embedded enforcement presented for founder review.** |
| 2026-07-18 | 3 | A7 Memory (S9) | MERGED | Founder-reviewed at the gate: **retrieval ranking verified (active-only pre-filter, docs/33 scoring, active-time recency, deterministic order, `max_k` clamp) and no-PII-embedded enforcement verified (verification-not-redaction via the merged S7 scanner, zero side effects on block, local-only embedder by construction, pin defense-in-depth); INV-MEM-1..4 covered** (this MERGED review is the founder review, per the auto-mode contract stage). Squash-merged `feat/a7-memory` (aec20fd); **579 tests green, main CI green** (gate 2 harness OK). **S9 is real, the Memory surface (docs/40 §6) backed by real code** → S10 Capability Framework builds against live retrieval; open follow-ups: additive `Config.memory` accessor to feed `RetrievalWeights` (memory RISKS R9, A2 cross-note); nightly perf tier watches exact-ranking scale (memory RISKS R6). |
| 2026-07-18 | 3 | A8 Framework (S10) | START | Founder go-ahead. Branch `feat/a8-framework` off `main` at A7 merge (f030d1a). Builds docs/13 + docs/04 §5–§7 against the fully LIVE stack — real Config (IF-2), Ledger (IF-1), Security (IF-3), Lifecycle vocabulary (IF-4), Router (S8), Memory (S9) — no stubs; A11 `FakeProvider` is every transport. |
| 2026-07-18 | 3 | A8 Framework (S10) | CONTRACTS-CLEARED | S10 contract docs (f1f05e6) written and self-cleared in **auto mode via the docs/56 consistency check** — no separate founder-clearance gate ran; founder review deferred to the merge gate (A5..A7 precedent). Key resolutions (IMPLEMENTATION §6): state→workflow table as injected DATA (S12/A9 own the rows); **Lifecycle consumed as vocabulary only — the runner never calls `transition` (no gate advances itself)**; model family = leading alphabetic token of the id (A2 cross-note: additive `Model.family`); the critic tier is knowable only post-call (recorded in result + event payload); S8 telemetry appends are observability, not venture state (INV-WF-1 taxonomy); A8 freezes the neutral-spec FORMAT (A9 fills the six contracts in Phase 5); the OpenCode adapter is a deterministic *generator*, never a harness/model invocation. |
| 2026-07-18 | 3 | A8 Framework (S10) | INTERFACE-FREEZE | **IF-5 frozen** (docs/43 §3, docs/52 §12): `Workflow.run(state, venture) -> WorkflowResult` + `Capability.produce(input) -> Artifact` + `Critic.critique(artifact) -> Critique` + the `CapInput`/`Artifact`/`Critique`/`WorkflowResult` shapes + INV-WF-1..3 semantics + the neutral `*.agent.md` section format (capabilities/API.md). Additive v1 seams (docs/43 §7): `WorkflowSpec.require`, `critique(…, spec=)`, further harness generators. **Unlocks A9 Content (S11) and A10 Conductor (S12).** |
| 2026-07-18 | 3 | A8 Framework (S10) | (impl, pre-gate) | Tests-first red (db3572e; 49 S10 tests — 48 NotImplementedError-for-the-right-reason + 1 trivially-green static import scan) → green (335cdef): the 5-beat runner with **structural beat isolation** (PREPARE/PRODUCE/CRITIQUE cannot reach vault or `Ledger.append`; CHECKPOINT is write-then-append with artifact rollback; domain events are **state-neutral by construction** — `to_state` never set); bounded PRODUCE retries (RouterError only; PII hard-stops propagate unretried; exhaustion = `BeatFailed` with **zero mutation proven incl. registry replay**, 10-seed property); the **INV-WF-2 ladder decided by what actually answered** (tier 1 diff-family / tier 2 same-family-diff-model / tier 3 deterministic spec-derived checklist on router exhaustion OR self-critique — never fails the run; tier recorded in result + event payload); INV-WF-3 by construction (`WorkflowResult` refuses `critique=None`); no-authority guards (registry refuses `AUTHORIZATION_REQUIRED`/unknown event types; static import scan: no governance/lifecycle imports); S9 `scope=` seam enforced; neutral-spec loader (fail-closed format matrix) + byte-deterministic OpenCode generator + `adapters/harness/opencode/generate.py` CLI. Live-S5 integration: **gate-never-advances-itself** (replay unchanged after a full run; only the explicit `Lifecycle.transition` moves state). Full CI green (**628 tests**, gate 2 harness OK). **HALT at merge gate — beat-isolation code + Critic degrade ladder presented for founder review.** |
| 2026-07-18 | 3 | A8 Framework (S10) | MERGED | Founder-reviewed at the gate: **beat isolation verified (structural by construction, fail-closed both directions with rollback, state-neutral events proven by replay, zero mutation on total provider failure) and the Critic ladder verified (tier by what actually answered, self-critique refused, always-available tier-3 floor, tier recorded for drift detection); INV-WF-1..3 covered** (this MERGED review is the founder review, per the auto-mode contract stage). Follow-up noted by the founder: **replace the family-derivation heuristic with the additive `Model.family` field when A2's accessor lands** (capabilities RISKS R3, joins router R9 + memory R9 on the A2 cross-note list). Squash-merged `feat/a8-framework` (8b4067b); **628 tests green, main CI green** (gate 2 harness OK). **S10 is real, IF-5 backed by real code — all five docs/43 §3 milestones frozen AND real** → A9 Content (S11) writes the six neutral specs against the frozen loader format; A10 Conductor (S12) composes the runner with live Lifecycle at the gates. |
| 2026-07-19 | 3 | A2 accessors (S3) | START | Founder go-ahead: one light branch `feat/a2-accessors` off `main` at A8 merge (6b87504) for the three open A2 cross-notes — **all additive per docs/43 §7, no consumer bump, no frozen signature altered**; no contract stage (no new subsystem; the three seams were already specified verbatim in router/memory/capabilities RISKS + IMPLEMENTATION cross-notes). Tests-first. |
| 2026-07-19 | 3 | A2 accessors (S3) | (impl, pre-gate) | Tests-first red (4bb426f; 10 tests — NotImplementedError skeletons + strict-schema/static-scan reds) → green (a64fca2): **`Config.models()`** (sorted catalog listing) with the router degrade extension moved onto it — `chain._catalog_ids` + the `config._models` private reach DELETED, a static test keeps `router/` clean (**router R9 retired**); **`Config.memory` → `MemoryConfig`** from routes.yaml's new strict-key `memory:` block (committed with docs/33 defaults) + `RetrievalWeights.from_config` field-for-field wiring (**memory R9 retired**); **`Model.family`** — explicit `models.yaml` key wins, else the canonical `contracts.default_family` derivation (ONE home, used by S3's loader); the critic's id parse DELETED, the tier decision follows the catalog via the injected `family_of` seam (`Workflow`/`Critic` additive kwarg; wiring passes `config.get_model(mid).family`) — proven authoritative in both disagree directions (**capabilities R3 retired**, the founder's A8-gate follow-up). Docs synced: config/API.md + docs/40 §1 additive notes; the three RISKS rows struck-through-retired; IMPLEMENTATION resolutions finalized. Full CI green (**638 tests**, gate 2 harness OK). **HALT at merge gate — awaiting founder review.** |
| 2026-07-19 | 3 | A2 accessors (S3) | MERGED | Founder-reviewed at the gate: **all three additive accessors clean, both interim reads (`_catalog_ids`, the critic string-parse) deleted, `Model.family` tier decision proven in both disagree directions, additive-only per docs/43 §7 confirmed.** Squash-merged `feat/a2-accessors` (9bb1df7); **638 tests green, main CI green** (gate 2 harness OK). The A2 cross-note list is **empty** — router R9, memory R9, capabilities R3 all retired; docs/40 §1 + config/API.md carry the additive notes. Wave-4 path stays: A9 Content (S11) + A10 Conductor (S12). |
| 2026-07-19 | 5 | A9 Content (S11) | START | Founder go-ahead. Branch `feat/a9-capabilities` off `main` at the A2-accessors merge (bbf644a). Deliverable is DATA, not code: the six neutral `agents/*.agent.md` contracts against A8's frozen loader format (IF-5). **No docs/56 four-doc contract stage ran — the specs ARE the contracts** (docs/13's frozen table is the source of truth; the docs/54 §S11 acceptance rows are the consistency check, encoded as tests). Tests-first. |
| 2026-07-19 | 5 | A9 Content (S11) | (impl, pre-gate) | Tests-first red (38 S11 tests, all `SpecInvalid` against the Phase-0 stubs — the red commit message miscounted 33; correct count 38) → green (c1d0a15): the six contracts written — **Scout** (reachability-as-hypothesis R-REACH-HYP + cold-start KPI grace; never self-admits), **Analyst** (evidence bar BEFORE spend R-EVIDENCE-GATE; interview PII → `.private.md` via CHECKPOINT, never embedded), **Builder** (staging autonomous; prod deploy + billing = two separate two-key RED founder requests R-CHARGE), **Growth** (DRAFTS ONLY; founder-wide send budget R-SEND-BUDGET; partner recruitment starts in SHAPING R-PARTNERS), **Librarian** (salvage asset types incl. anti-patterns R-SALVAGE-TYPES; reversible consolidation INV-MEM-3; doctrine PROPOSE-only), **Critic** (cross-family degrade ladder, tier always recorded R-CRITIC-DEGRADE; READ lesson, **writes nothing**). Verified: docs/13 memory-scope + outputs tables **verbatim** in the parsed specs; the framework refuses out-of-scope writes per capability (S9 seam; the Critic refused for any tagged write); **full 5-beat dry-run per capability over the live stack** (docs/13 DoD); OpenCode generation over the real six. One A8 test repointed (empty-stub fail-loud now pinned via fixture — the repo stubs are real specs; TESTPLAN synced). Full CI green (**676 tests**, gate 2 harness OK). **HALT at merge gate — awaiting founder review.** |
| 2026-07-19 | 5 | A9 Content (S11) | MERGED | Founder-reviewed at the gate: **all six capability specs load under the frozen loader, memory scopes match docs/13 verbatim, out-of-scope writes refused (Critic refused any write), each capability dry-runs the 5 beats, v1.1 specifics asserted** (this MERGED review is the founder review). Squash-merged `feat/a9-capabilities` (1b6ae75); **676 tests green, main CI green** (gate 2 harness OK). **S11 is real — the six neutral contracts are live data behind IF-5** → the OpenCode adapter can generate real harness files; **A10 Conductor (S12) is the last Wave-4/5 subsystem**: the command surface composing runner + live Lifecycle at the gates, owning the real state→workflow table (docs/10, docs/40 §8). |
| 2026-07-21 | 7 | A10 Conductor (S12+S13) | START | Founder go-ahead. Branch `feat/a10-conductor` off `main` at the A9 merge (def1d5a). Built LAST per docs/10 — integrates the finished stack (no stubs): real Config/Ledger/Registry/Lifecycle/Gov/Security/Router/Memory/Workflow + the six merged A9 contracts. Founder instruction on record: any live-stack mismatch is fixed tests-first on this branch and flagged at the gate. |
| 2026-07-21 | 7 | A10 Conductor (S12+S13) | CONTRACTS-CLEARED | Both four-doc sets (9fc3fe3) written and self-cleared in **auto mode via the docs/56 consistency check** — founder review deferred to the merge gate (A5..A9 precedent). Key resolutions (conductor/IMPLEMENTATION §6): **single token consumption at the owner** (transitions pass the token through to S5's boundary; the conductor never pre-authorizes gate rows); `spec_approved` appended under the SAME gate token before the SHAPING→BUILDING transition (one decision, one consumption); v1 workflow commands = the matrix's `shape`/`build` (other states via the S10 surface until the scheduler era); the **additive `artifact_produced` event** (IF-1's recorded evolution rule; docs/41 §2 updated same-PR); `calibrate` is a pure read; **v1 has NO real deploy/billing/send effect — the authorization boundary is the end of the line** (the recorded token-carrying events are the future pipelines' integration points); recommendations are advisory ledger folds. |
| 2026-07-21 | 7 | A10 Conductor (S12+S13) | (impl, pre-gate) | Tests-first red (31 tests; 30 NotImplementedError-for-the-right-reason + 1 trivially-green static scan) → green (4c8b7b1): **S12** — the `Conductor.command` chokepoint (classify→owner-guards→act→append→regenerate; dispatch stateless, the ledger the only memory), call-through proven twice (static rule-absence scan + SpyGov/SpyLifecycle call-path tests incl. consumed-exactly-once), the `gate` command briefing FIRST (fail-closed `NoCriticForGate`) and appending the ONE `gate_decision` with `critic_tier`, two-key deploy/billing refused on token-alone, the REAL state→workflow table over the six A9 specs (state-neutral `artifact_produced` checkpoints); **S13** — six pure ledger folds (board/metrics/triaged-silence-valid daily/gate/killday-never-drops/calibration), GateBrief critic REQUIRED by construction, purity proven by recompute + snapshot/restore + write-nothing. **Integration: the docs/10 DoD full dry-run Capture→GRADUATED through the chokepoint — every RED point halted first at the authorization boundary, zero real spend/send/deploy (INV-TEST-SAFE)**; INV-COND-3 crash probes (fresh-conductor identity; a dying recorder append loses nothing). **Live-stack wiring surfaced ZERO subsystem mismatches — no fixes needed; the frozen seams composed as specified.** Full CI green (**707 tests**, gate 2 harness OK). **HALT at merge gate — command-dispatch chokepoint + Gate Brief assembly presented for founder review; no integration fixes to report.** |
| 2026-07-22 | 7 | A10 Conductor (S12+S13) | MERGED | Founder-reviewed at the gate: **chokepoint is call-through (proven twice: static scan + call-path spies), token consumption exactly-once, INV-COND-1/2/3 all hold, Gate Brief requires its Critic field by construction, and the full Capture→GRADUATED dry-run runs end-to-end with zero real effects possible (INV-TEST-SAFE a code fact); zero integration mismatches** (this MERGED review is the founder review, per the auto-mode contract stage). Squash-merged `feat/a10-conductor` (69dcdcb); **707 tests green, main CI green** (gate 2 harness OK). **S12+S13 are real → EVERY subsystem S2–S15 is now real on an always-green `main`**: the whole docs/10 loop — capture, framing, admission, validation, gates with mandatory critic takes, shaping/build workflows, the two-key RED boundary, graduation, projections — runs through the single chokepoint over the live stack. The factory software is complete to the v1 boundary; what remains is phase-exit hardening (activating the placeholder CI gates 1/5/9/10, docs/63) and the ops layer (real transports/pipelines consuming the recorded token-carrying events). |
| 2026-07-22 | 7 | Phase-7 hardening | (impl, pre-gate) | Founder go-ahead. Branch `feat/phase7-hardening` off `main` at the A10 merge (0fad53c). **ALL 10 docs/63 merge gates are now LIVE** — the four placeholders activated as mechanical checks: **gate 1** `scripts/architecture_check.py` (13 packages × 4 contract docs live, zero Phase-0 scaffolds; every docs/40 §1–§10 frozen surface resolves in code — remove/rename = ICR drift, fail; presence-level check, full signature-parse noted as the docs/62 tooling follow-up); **gate 5** `scripts/anticoupling_check.py` (the docs/43 §5 allowed-import DAG as data, exhaustive over all 14 packages; conductor = the documented integrator exception); **gate 9** `scripts/ownership_check.py` (the docs/51 longest-prefix ownership map; all 273 tracked files owned, orphans fail; the changed-paths-vs-author half stays with merge-gate review — no PR identity in the local flow, noted in-script); **gate 10** `scripts/determinism_check.py` (the 10 deterministic packages may never import router/memory/capabilities — INV-DET; conductor documented as the composing exception, held by its INV-COND-1 tests). **Full CI green with all 10 gates: 707 tests + 4 new mechanical checks — ZERO findings surfaced** (the discipline held all along; now it is enforced, not remembered). **HALT at merge gate — awaiting founder review.** |
| 2026-07-22 | 7 | Phase-7 hardening | MERGED | Founder-reviewed at the gate: **all 10 docs/63 gates live, zero findings — discipline held, now mechanically enforced** (this MERGED review is the founder review). Squash-merged `feat/phase7-hardening` (d2627f0); **main CI green with all 10 gates active** (707 tests + the 4 mechanical checks). |
| 2026-07-22 | 7 | Phase 7 | PHASE-EXIT | **Tagged `phase-7`** (docs/63 release tagging). Exit state: every subsystem S2–S15 real on an always-green `main`; IF-1..IF-5 + the Memory surface frozen and code-backed; the six neutral capability contracts live; the full docs/10 loop (Capture→GRADUATED, zero real effects — INV-TEST-SAFE a code fact) runs through the single chokepoint; **the complete docs/63 merge gate is armed** (all 10 gates mechanical). **HALT — next phase (the ops layer: real transports/pipelines consuming the recorded token-carrying events under their two-key/RED discipline) opens on founder go-ahead.** |
| 2026-07-26 | 7 | Founder CLI (S12 shell) | START | Founder go-ahead (post phase-7 exit). Branch `feat/founder-cli` off `main` at the phase-7 tip (8f6f93d). A thin terminal shell over `Conductor.command` + the S13 projections for driving the by-hand daily + kill-day loop (docs/05) — **additive, no new subsystem, no frozen surface changed** (docs/43 §7; `command` stays frozen). No docs/56 four-doc contract stage — the CLI adds no rule; the test suite IS the spec (A2-accessors precedent). Tests-first. |
| 2026-07-26 | 7 | Founder CLI (S12 shell) | (impl, pre-gate) | Tests-first red (7 tests, both entry points `NotImplementedError` — an interrupted prior session left the suite + skeleton) → green: **`build_factory`** (the composition root wiring the fully live stack — real Config over committed `config/`, Ledger/Registry/Gov(wall clock)/Lifecycle/Security/Memory/Router/Workflow/Conductor; injection seams for embedder/transports) + **`main`** (argparse subcommand → ONE `Conductor.command(name, args, tok)` → print → exit; **no rule, no durable state — INV-COND-1/3 by construction**, a fresh process per invocation over the same ledger). RED commands (`admit`/`gate`/`kill`) halt tokenless with the OWNER's verbatim refusal (exit 1); `--approve` mints the single-use grant at the Gov boundary (`gov.grant`, scope == the owner's action name) and passes it through — minted+consumed inside the one process. INV-COND-2 through the shell: an unbriefable `gate` refuses even WITH `--approve` (`NoCriticForGate`), advances once a critic take exists. Two fail-closed seams stand in for ops wiring (`NoTransport`/`NoEmbedder`, neither on any v1 command path). Owner refusals caught as `(ConductorError, ProjectionsError)` — `NoCriticForGate` is a S13 `ProjectionsError` that exits `command()` unwrapped. Repo conventions held: `tok` not `token` (secret scan), the `security.scan` string kept out of `conductor/` (INV-COND-1 static scan), conductor = the anti-coupling integrator exception. Full CI green with all 10 gates (**714 tests**, +7); conductor/API.md carries the additive CLI note. **HALT at merge gate — awaiting founder review; no merge to `main`.** |
| 2026-07-27 | 7 | Founder CLI (S12 shell) | MERGED | Founder-reviewed at the gate: **`main` dispatch confirmed a thin pass-through — parse → `_translate` (pure arg marshalling, no classification/legality/guards) → ONE `Conductor.command(name, args, tok)` → print; the `test_call_through_no_local_rules` static scan keeps `conductor/` free of AuthClass/MATRIX/TRANSITIONS. RED (admit/gate/kill) halts tokenless with the owner's verbatim refusal (exit 1, venture unmoved); `--approve` mints the single-use grant at the Gov boundary (`gov.grant`, scope == owner action name) and passes it through — owner consumes exactly once (INV-GOV-1/3); the CLI never builds/authorizes/consumes a token.** Squash-merged `feat/founder-cli` (b8a5a14); **main CI green with all 10 gates (714 tests, +7)**; branch deleted. The by-hand founder driver for the docs/05 loop is live on `main`; next phase — the ops layer (real transports/pipelines) — still opens on founder go-ahead. |

---

## Open Blocking Impossibilities
_None._

## Interface freezes recorded
- **IF-1 — Ledger/Registry + Event catalog** (2026-07-04): envelope (docs/41 §1) + event vocabulary (docs/41 §2)
  + `append/read/replay/snapshot/restore` + `Registry.get/query`. Consumers: A4, A5, A7, A10, A11.
- **IF-2 — Config + LLMClient (COMPLETE)** (Config half 2026-07-04; LLMClient half 2026-07-11):
  `get_route/get_model/get_provider/profile/budgets` + Route/Model/Provider/Budgets;
  `LLMClient.call(role, messages, tools?, require?) -> LLMResponse` + `Require`/`LLMResponse` +
  `Adapter.complete` (docs/40 §1/§5). Consumers: A1 preflight, A7 Memory, S10 Framework.
- **IF-3 — Governance + Security seam** (2026-07-07): Governance `classify/authorize/envelope_open/spend/send_budget_remaining`
  + Security `redact/scan/tag` + `Context.contains_pii` cloud-route guard (docs/40 §4). Consumers: A6/S8 Router, A7 Memory.
- **IF-4 — Lifecycle transition API + docs/42 invariants** (2026-07-10): `can_transition/transition/slots/clock`
  (docs/40 §3) + the docs/42 §3 table/§2 WIP/§4 rules/§5 pivot (INV-SM-1..6); additive `pivot/grant_omw/pause/resume`.
  Consumers: S10 Capability Framework, S12 Conductor.
- **Memory surface** (2026-07-16, with the A7 contract stage — docs/12 "frozen early", docs/40 §6):
  `Memory.retrieve/write_lesson/consolidate/reindex` + `Embeddings.embed` + the
  TaskContext/Lesson/ScoredLesson/WorkingSet/ConsolidationReport shapes (memory/API.md);
  additive seams `scope=` / `RetrievalWeights` injection. Consumers: S10 Framework, S12 Conductor.
- **IF-5 — Workflow runner** (2026-07-18, with the A8 contract stage): `Workflow.run(state, venture)
  -> WorkflowResult` + `Capability.produce` + `Critic.critique` + CapInput/Artifact/Critique/
  WorkflowResult shapes + INV-WF-1..3 semantics + the neutral `*.agent.md` format
  (capabilities/API.md). Consumers: A9 Content (S11), A10 Conductor (S12).
- All five docs/43 §3 interface-freeze milestones are now recorded.

## Current phase
**Phase 5 — Wave 4 in progress; A8 Capability Framework (S10) MERGED on `main`** (S2, S3,
S4, S5, S6+S7, S8, S9, S10, S14+S15; main always-green, 628 tests). **All five docs/43 §3
interface freezes are recorded AND backed by real code** → next on the plan: A9 Content
(S11 — the six neutral `agents/*.agent.md` specs against the frozen loader format) and
**A10 Conductor (S12) + Projections (S13) MERGED on `main`** (69dcdcb; main
always-green, **707 tests**). **Every subsystem S2–S15 is real**: the full docs/10 loop
(Capture→GRADUATED, zero real effects — INV-TEST-SAFE a code fact) runs through the
single chokepoint over the live stack; IF-1..IF-5 + the Memory surface all frozen AND
backed by real code; the six neutral capability contracts are live data. **The factory
software is complete to the v1 boundary and PHASE 7 IS EXITED (tag `phase-7`,
d2627f0):** every subsystem real, every interface frozen and code-backed, the full
factory loop live behind the chokepoint, and **all 10 docs/63 merge gates mechanically
enforced**. The build ledger + always-green `main` remain the complete resumable state
(docs/70 §9). **Next phase — the ops layer** (real transports/pipelines consuming the
recorded token-carrying events under their two-key/RED discipline) — **opens on founder
go-ahead.**
